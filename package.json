{
  "name": "react-analyzer",
  "private": true,
  "scripts": {
    "prebuild": "pnpm lint:lib",
    "build": "turbo run build --filter='./lib'",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:lib": "turbo run lint --filter='./lib'",
    "publish": "turbo run publish --filter='./lib"
  },
  "devDependencies": {
    "eslint": "^9.20.1",
    "turbo": "^2.4.2",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.4.0",
  "engines": {
    "node": ">=18.0.0"
  }
}