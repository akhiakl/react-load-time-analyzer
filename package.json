{
  "name": "react-analyzer",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "publish": "turbo run publish --access public"
  },
  "devDependencies": {
    "eslint": "^9.20.1",
    "turbo": "^2.4.2",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.4.0",
  "engines": {
    "node": ">=18.0.0"
  }
}